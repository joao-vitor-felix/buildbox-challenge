import { GlobalStyles } from "@/styles/GlobalStyles";
import { Theme } from "@/styles/Theme";
const App = () => {
  return (
    <Theme>
      <GlobalStyles />;
    </Theme>
  );
};

export default App;
